# Level 7 → 8

## Concept
Searching inside a large file for a specific word.

## Solution
```bash
grep "millionth" data.txt
```

## What I Learned
- `grep "word" filename` finds lines containing a word
- Much faster than reading a large file manually
