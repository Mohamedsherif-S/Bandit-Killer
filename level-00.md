# Level 0 → 1

## Concept
SSH basics — connecting to a remote server.

## Commands Used
```bash
ssh bandit0@bandit.labs.overthewire.org -p 2220
cat filename
```

## What I Learned
- SSH syntax: `ssh username@host -p port`
- `cat` reads file contents
